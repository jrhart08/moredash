{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es2015",
    "outDir": "./lib",
    "declaration": true,
    // emit .d.ts files
    "emitDeclarationOnly": true,
    // don't emit .js files (let babel do it)
    "isolatedModules": true
  },
  "include": [
    "src/**/*"
  ]
}
